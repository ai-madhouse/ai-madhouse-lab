# Copy to .env.local for local development.
# In production, prefer an environment file owned by root (e.g. systemd EnvironmentFile).

# --- Secrets (REQUIRED) ---
# Use long, random values. Do NOT commit real secrets.
# Example generation: openssl rand -base64 48
AUTH_SECRET=dev-secret-change-me-please-1234567890
REALTIME_SECRET=dev-realtime-secret-change-me-please-1234567890

# --- App config ---
DB_PATH=data/app.db

# Next.js server port
PORT=3000

# Realtime WebSocket server port
REALTIME_PORT=8787
